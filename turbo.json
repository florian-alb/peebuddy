{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "^db:generate"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["POSTGRES_PRISMA_URL", "GITHUB_CLIENT_ID", "GITHUB_CLIENT_SECRET"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "dependsOn": ["^db:generate"],
      "cache": false,
      "persistent": true,
      "env": ["POSTGRES_PRISMA_URL", "POSTGRES_URL_NON_POOLING", "GITHUB_CLIENT_ID", "GITHUB_CLIENT_SECRET"]
    },
    "db:generate": {
      "cache": false,
      "env": ["POSTGRES_PRISMA_URL", "POSTGRES_URL_NON_POOLING"]
    },
    "db:migrate": {
      "cache": false,
      "persistent": true,
      "env": ["POSTGRES_PRISMA_URL", "POSTGRES_URL_NON_POOLING"]
    },
    "db:deploy": {
      "cache": false,
      "env": ["POSTGRES_PRISMA_URL", "POSTGRES_URL_NON_POOLING"]
    }
  }
}
